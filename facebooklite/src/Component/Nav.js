import React, { Component } from 'react';

export default class Nav extends Component{
    render() {
        return (
          <div>
              <button variant="primary">Sam</button>
              <button variant="primary">Arthur</button>
              <button variant="primary">Maxime</button>
          </div>
        );
      }
}