import React, { Component } from 'react';

export default class ButtonChange extends Component{
    render() {
        return (
          <div>
              <button variant="primary">Change Style!</button> 
          </div>
        );
      }
}